name: Build and Install Package

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v2
        with:
          environment-file: env.yml
          activate-environment: ml_project_env
          use-mamba: true  # Faster and more stable

      - name: Display Directory Tree Before Build
        run: tree .

      - name: Build the Package
        run: python setup.py sdist bdist_wheel

      - name: Display Directory Tree After Build
        run: tree .

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            dist/
            env.yml  # Ensures env.yml is available in the next job

  install:
    runs-on: ubuntu-latest
    needs: build  # Ensures 'build' job runs first
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: dist/  # Explicitly set the path

      - name: Verify Downloaded Artifacts
        run: |
          ls -R 
          if [ ! -f env.yml ]; then echo "❌ env.yml is missing!" && exit 1; fi

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v2
        with:
          environment-file: env.yml
          activate-environment: ml_project_env
          use-mamba: true

      - name: Install Package
        run: pip install *.whl

      - name: Run Data Ingestion Script
        run: python scripts/ingest_data.py --output-path data/processed_data.csv

      - name: Run Model Training Script
        run: python scripts/train.py --input-path data/processed_data.csv --output-path model_registry/trained_model.pkl --log-level DEBUG --log-path logs/train.log --no-console-log

      - name: Run Scoring Script
        run: python scripts/score.py --model-path model_registry/trained_model.pkl --data-path data/processed_data.csv
